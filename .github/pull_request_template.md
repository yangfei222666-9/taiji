## What changed

<!-- Name the module/file path and the concrete behavior change. -->

## Why

<!-- Describe the problem this fixes. Do not use broad platform claims. -->

## Evidence

- [ ] Event flow, audit log, or equivalent artifact path is included when this affects runtime, learning, automation, or verification.
- [ ] New or changed behavior has a reproducible command, test, or verifier.
- [ ] No stale artifact is being reused as proof of a new result.

## Validation

<!-- Paste commands and outcomes. Example: `python -m pytest tests -q --tb=short` -->

- [ ] Tests or verifiers passed.
- [ ] Failure or blocked state is explicitly recorded when validation cannot pass.
- [ ] Secret scan does not print or expose credential values.

## Boundaries

- [ ] This change does not grant paper-buy, trade, sell, promote, or judgment authority from a learning-only path.
- [ ] `degraded` and `learning_only` outputs remain blocked from judgment or execution.
- [ ] Any provider, relay, key, DNS, quota, auth, or model-route issue is reported as a blocker instead of being hidden.

## Rollback

<!-- State the smallest safe rollback step. -->
